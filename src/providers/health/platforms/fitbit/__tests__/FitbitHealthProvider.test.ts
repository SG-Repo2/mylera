import { FitbitHealthProvider } from '../FitbitHealthProvider';

describe('FitbitHealthProvider', () => {
  describe('token refresh', () => {
    test('Test token refresh for Fitbit', async () => {
      // Test implementation
    });
  });
});
